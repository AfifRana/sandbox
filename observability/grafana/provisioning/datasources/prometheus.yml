apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    # Fixed UID so provisioned dashboards can reference it directly.
    uid: PBFA97CFB590B2093
    isDefault: true
    editable: false
